import { Redirect } from 'expo-router';

export default function SettingsIndex() {
  // Settings index is shown in the tab, so we don't need a separate screen
  // This is just a placeholder for the settings list which is rendered in the tab
  return null;
}
